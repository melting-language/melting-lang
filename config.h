#ifndef CONFIG_H
#define CONFIG_H

/* USE_* are set by Makefile via -D (e.g. -DUSE_SQLITE -DUSE_GUI) or by CMake from config.h.in */

#define PROJECT_VERSION_MAJOR 1
#define PROJECT_VERSION_MINOR 3
#define PROJECT_VERSION_PATCH 2

#endif
